A comparator wraps an operator selector and a human-readable description for it.

Its main usage is through a Property object, which determines a set of corresponding comparators for a selected property.

Instance Variables
	description:		<String>
	selector:			<Symbol>

